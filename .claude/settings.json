{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "filepath=\"$CLAUDE_TOOL_INPUT_FILE_PATH\"; if [ -n \"$filepath\" ] && echo \"$filepath\" | grep -qE '\\.php$'; then php -l \"$filepath\" 2>&1 || echo 'PHP syntax error detected'; fi"
          }
        ]
      }
    ]
  }
}
